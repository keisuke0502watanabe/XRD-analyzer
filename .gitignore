# OS / editor junk
.DS_Store

# Local scratch archives
gh.tar.gz

# Local tooling / machine-specific config
.claude/

# local sample data (not committed)
_samples/

# Local DB backups (large, not committed)
db_backup/

# Quarantine for files staged for manual deletion
_to_delete/
